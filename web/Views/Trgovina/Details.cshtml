@model SeminarskaNaloga.Models.Trgovina

@{
    ViewData["Title"] = "Details";
}

<div class="col-md-4 offset-md-4 text-center">
    <h4>Trgovina</h4>
    <hr />
    <dl class="row">
        <div class="col-md-4">
        <dd>
            <div>
                <img src="@Html.DisplayFor(model => model.img)" style="max-width:200px">
            </div>
            
        </dd></div>
        <div class="col-md-8">
        <dt>
            @Html.DisplayNameFor(model => model.ime)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.ime)
        </dd>
        </div>
    </dl>
<div>

</div>    
<div>
    @if(User.IsInRole("Lastnik") || User.IsInRole("Admin"))
    {
    <a asp-action="Edit" asp-route-id="@Model?.TrgovinaId">Edit</a>
    }
    @if(User.IsInRole("Admin"))
    {
    <a asp-action="Delete" asp-route-id="@Model?.TrgovinaId">Delete</a>
    }
    <a asp-action="Index">Back to List</a>
</div>
